#include <bits/stdc++.h>
using namespace std;

vector<int> bin_search(vector<vector<int>> v, int a){
    left 
}

int main() {
    int n; cin >> n;
    vector<int> ans;
    
    for(int i = 0; i < n; i++){
        int m; cin >> m;
        ans.push_back(m);
    }
    
    int h, w; cin >> h >> w;
    vecto<vector<int>> v;
    
    for(int i = 0; i < h; i++){
        for(int j = 0; j < w; j++){
            int m; cin >> m;
            v[i][j] = m;
        }
    }

}